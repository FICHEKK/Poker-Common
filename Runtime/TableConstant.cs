public static class TableConstant
{
    /// <summary>Time duration a player is allowed to decide (in seconds).</summary>
    public const float PlayerTurnDuration = 10;
    
    /// <summary>Extra overtime a player is allowed to decide (in seconds).</summary>
    public const float PlayerTurnOvertime = 1;
    
    /// <summary>Pause duration in-between 2 rounds (in milliseconds).</summary>
    public const int RoundFinishPauseDuration = 5000;
    
    /// <summary>Pause duration for a single card to flip over (in milliseconds).</summary>
    public const int PausePerCardDuration = 500;
}